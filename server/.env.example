NODE_ENV=development
PORT=3001
JWT_SECRET=troque-esta-chave
JWT_REFRESH_SECRET=troque-esta-chave-refresh
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tagri_db
DB_USER=postgres
DB_PASS=postgres
ALLOWED_ORIGINS=http://localhost:5173

# Master database for multitenancy control
MASTER_DB_URL=postgres://postgres:postgres@localhost:5432/postgres

# Tenant database configuration  
TENANT_DB_HOST=localhost
TENANT_DB_PORT=5432
TENANT_DB_USER=postgres
TENANT_DB_PASS=postgres
TENANT_DB_PREFIX=tagri_t_

# Encryption key for tenant passwords (AES-256-GCM)
MASTER_KMS_KEY=troque-esta-chave-super-secreta

# Master database for tenancy management (can be the same as above)
MASTER_DB_URL=postgres://postgres:postgres@localhost:5432/tagri_db

# Tenant database configuration
TENANT_DB_HOST=localhost
TENANT_DB_PORT=5432
TENANT_DB_USER=postgres
TENANT_DB_PASS=postgres
TENANT_DB_PREFIX=tagri_t_

# Encryption key for tenant passwords (AES-256-GCM)
MASTER_KMS_KEY=troque-esta-chave-super-secreta
